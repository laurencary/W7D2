<h2>Here are the bands!</h2>

<ul>
    <% @bands.each do |band| %>
        <li><a href="<%= band_url(band) %>"><%= render 'band', band: band %></a></li>
    <% end %>

</ul>

<br>
<a class="button_link" href="<%= new_band_url %>">Add Band</a>