<h2>Add a Band</h2>

<form action="<%= bands_url %>" method="POST">
    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
    <label for="name">Name: </label>
    <input type="text" name="band[name]" id="name">
    <br>
    <br>
    <input type="submit" value="Add band">
</form>